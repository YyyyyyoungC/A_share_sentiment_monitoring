你好。整体方法论我认可，不打算推翻重来，因初始提示未明确执行规范，导致在 Wind Alice  万得金融数据环境下积分消耗过高，需要你帮我做定向优化。

**一、背景与优化目标**

1. **补充执行规范**：当时给你的提示词里遗漏了执行规范章节，需要你把它补进体系（公式说明书与计算脚本都要同步更新，版本号升为v1.1.0）。
2. **数据源替换为 Wind Alice 万得金融数据**：体系当前基于东财/腾讯/新浪公网接口，现统一替换为 Wind Alice 万得金融数据。**注意：Wind Alice 万得金融数据按调用消耗积分，请在迁移时同步做积分成本优化**（见第三条要求）。

**二、需要补入的执行规范（原文，直接融入体系）**

- 执行时间：每周五 18:00（收盘后），盘中数据一律禁用；当日收盘未完成时则暂停生成报告，并向我进行询问。
- 数据源：强制使用 Wind Alice 万得金融数据，未经我书面同意不得切换或混用。
- 异常处理：
  a. 若取数返回为空、积分不足或其他问题，第一时间暂停报告生成，绝不用替代值硬算；
  b. 主动与我沟通，说明异常的具体位置（数据源/表/字段/日期）；
  c. 提供多个解决方案及利弊说明（至少两个选项）；
  d. 必须经我确认后才能继续执行；
  e. 严禁自行决定关键问题（包括但不限于：更换数据源、修改权重/阈值/窗口、填补或估算缺失值）。

**三、本次优化新增的要求：Wind Alice 万得金融数据积分成本控制**

迁移到 Wind Alice 万得金融数据后，请在不违反体系原有数据纪律的前提下，设计降本措施。
核心优化目标：保留之前方法论、模型逻辑、数据纪律的前提下，将原逐股遍历计算的高消耗模式，改为调用Wind Alice 万得金融数据的全市场预聚合指标，大幅降低积分消耗。
由于Wind Alice万得金融数据采用的是积分制度，下面只是我列举的优化积分方案，同样需要你设计降本措施，尽可能降低积分成本。

优化积分方案：
1. **增量更新**：历史已核验数据（如全指日线、已抓取的板块/风格快照）必须本地缓存并带SHA256校验，每次运行只增量抓取新交易日，禁止全量重复拉取历史。
2. **抓取频率匹配时点**：模型定义为周五18:00收盘后运行一次，请按"周频"设计调用计划，不要保留日频轮询逻辑。
3. **字段最小化**：只取六因子与周报栏目必需的字段，不取用不到的指标。
4. **失败即停不加试**：接口失败时按异常处理流程暂停，不设计自动重试风暴（避免无效调用烧积分）；如需重试，写入方案让我确认。
5.**历史回测专项优化**：历史复盘阶段优先调用 Wind 的**历史广度指标时间序列**（涨跌家数、均线占比、跌幅分布的历史序列），替代逐股回溯计算，大幅降低历史回测的积分消耗。
6.直接使用指数级现成汇总指标


**四、不得改变的内容（方法论底线，请原样保留）**

核心模型逻辑必须 100% 保留，不得为了降积分而修改公式本质。必须保留的核心包括：

1. 六因子结构与固定权重（净涨跌广度 20%、均线广度 15%、20 日动量 20%、个股急跌比例 15%、下行半波动 20%、融资强度 10%）；
2. 756 交易日滚动分位数归一化、半秩并列值处理、严格无前视的计算逻辑；
3. 「任一核心因子缺失则完整 S=NA，不填 0、不填 50、不重新分配权重」的铁律；
4. 情绪分 + 硬价格阈值的双轨独立预警机制，以及周报保留周内全部风险事件的规则；
5. 周报告完整结构：情绪指数、中证全指表现、预警、板块线、情绪线、资金流向、外围市场七大模块。

**五、交付物**
更新后的《A股情绪指数公式说明书》和《A股情绪指数计算脚本》，统一采用Markdown文件格式输出；